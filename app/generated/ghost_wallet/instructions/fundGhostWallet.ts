/**
 * This code was AUTOGENERATED using the Codama library.
 * Please DO NOT EDIT THIS FILE, instead use visitors
 * to add features, then rerun Codama to update it.
 *
 * @see https://github.com/codama-idl/codama
 */

import {
  combineCodec,
  fixDecoderSize,
  fixEncoderSize,
  getBytesDecoder,
  getBytesEncoder,
  getStructDecoder,
  getStructEncoder,
  getU64Decoder,
  getU64Encoder,
  transformEncoder,
  type AccountMeta,
  type AccountSignerMeta,
  type Address,
  type FixedSizeCodec,
  type FixedSizeDecoder,
  type FixedSizeEncoder,
  type Instruction,
  type InstructionWithAccounts,
  type InstructionWithData,
  type ReadonlyAccount,
  type ReadonlyUint8Array,
  type TransactionSigner,
  type WritableAccount,
  type WritableSignerAccount,
} from "@solana/kit";
import { GHOST_WALLET_PROGRAM_ADDRESS } from "../programs";
import { getAccountMetaFactory, type ResolvedAccount } from "../shared";

export const FUND_GHOST_WALLET_DISCRIMINATOR = new Uint8Array([
  119, 204, 155, 170, 80, 123, 87, 118,
]);

export function getFundGhostWalletDiscriminatorBytes() {
  return fixEncoderSize(getBytesEncoder(), 8).encode(
    FUND_GHOST_WALLET_DISCRIMINATOR,
  );
}

export type FundGhostWalletInstruction<
  TProgram extends string = typeof GHOST_WALLET_PROGRAM_ADDRESS,
  TAccountFunder extends string | AccountMeta<string> = string,
  TAccountWallet extends string | AccountMeta<string> = string,
  TAccountAction extends string | AccountMeta<string> = string,
  TAccountSystemProgram extends string | AccountMeta<string> =
    "11111111111111111111111111111111",
  TRemainingAccounts extends readonly AccountMeta<string>[] = [],
> = Instruction<TProgram> &
  InstructionWithData<ReadonlyUint8Array> &
  InstructionWithAccounts<
    [
      TAccountFunder extends string
        ? WritableSignerAccount<TAccountFunder> &
            AccountSignerMeta<TAccountFunder>
        : TAccountFunder,
      TAccountWallet extends string
        ? WritableAccount<TAccountWallet>
        : TAccountWallet,
      TAccountAction extends string
        ? WritableAccount<TAccountAction>
        : TAccountAction,
      TAccountSystemProgram extends string
        ? ReadonlyAccount<TAccountSystemProgram>
        : TAccountSystemProgram,
      ...TRemainingAccounts,
    ]
  >;

export type FundGhostWalletInstructionData = {
  discriminator: ReadonlyUint8Array;
  amount: bigint;
};

export type FundGhostWalletInstructionDataArgs = { amount: number | bigint };

export function getFundGhostWalletInstructionDataEncoder(): FixedSizeEncoder<FundGhostWalletInstructionDataArgs> {
  return transformEncoder(
    getStructEncoder([
      ["discriminator", fixEncoderSize(getBytesEncoder(), 8)],
      ["amount", getU64Encoder()],
    ]),
    (value) => ({ ...value, discriminator: FUND_GHOST_WALLET_DISCRIMINATOR }),
  );
}

export function getFundGhostWalletInstructionDataDecoder(): FixedSizeDecoder<FundGhostWalletInstructionData> {
  return getStructDecoder([
    ["discriminator", fixDecoderSize(getBytesDecoder(), 8)],
    ["amount", getU64Decoder()],
  ]);
}

export function getFundGhostWalletInstructionDataCodec(): FixedSizeCodec<
  FundGhostWalletInstructionDataArgs,
  FundGhostWalletInstructionData
> {
  return combineCodec(
    getFundGhostWalletInstructionDataEncoder(),
    getFundGhostWalletInstructionDataDecoder(),
  );
}

export type FundGhostWalletInput<
  TAccountFunder extends string = string,
  TAccountWallet extends string = string,
  TAccountAction extends string = string,
  TAccountSystemProgram extends string = string,
> = {
  funder: TransactionSigner<TAccountFunder>;
  wallet: Address<TAccountWallet>;
  action: Address<TAccountAction>;
  systemProgram?: Address<TAccountSystemProgram>;
  amount: FundGhostWalletInstructionDataArgs["amount"];
};

export function getFundGhostWalletInstruction<
  TAccountFunder extends string,
  TAccountWallet extends string,
  TAccountAction extends string,
  TAccountSystemProgram extends string,
  TProgramAddress extends Address = typeof GHOST_WALLET_PROGRAM_ADDRESS,
>(
  input: FundGhostWalletInput<
    TAccountFunder,
    TAccountWallet,
    TAccountAction,
    TAccountSystemProgram
  >,
  config?: { programAddress?: TProgramAddress },
): FundGhostWalletInstruction<
  TProgramAddress,
  TAccountFunder,
  TAccountWallet,
  TAccountAction,
  TAccountSystemProgram
> {
  // Program address.
  const programAddress = config?.programAddress ?? GHOST_WALLET_PROGRAM_ADDRESS;

  // Original accounts.
  const originalAccounts = {
    funder: { value: input.funder ?? null, isWritable: true },
    wallet: { value: input.wallet ?? null, isWritable: true },
    action: { value: input.action ?? null, isWritable: true },
    systemProgram: { value: input.systemProgram ?? null, isWritable: false },
  };
  const accounts = originalAccounts as Record<
    keyof typeof originalAccounts,
    ResolvedAccount
  >;

  // Original args.
  const args = { ...input };

  // Resolve default values.
  if (!accounts.systemProgram.value) {
    accounts.systemProgram.value =
      "11111111111111111111111111111111" as Address<"11111111111111111111111111111111">;
  }

  const getAccountMeta = getAccountMetaFactory(programAddress, "programId");
  return Object.freeze({
    accounts: [
      getAccountMeta(accounts.funder),
      getAccountMeta(accounts.wallet),
      getAccountMeta(accounts.action),
      getAccountMeta(accounts.systemProgram),
    ],
    data: getFundGhostWalletInstructionDataEncoder().encode(
      args as FundGhostWalletInstructionDataArgs,
    ),
    programAddress,
  } as FundGhostWalletInstruction<
    TProgramAddress,
    TAccountFunder,
    TAccountWallet,
    TAccountAction,
    TAccountSystemProgram
  >);
}

export type ParsedFundGhostWalletInstruction<
  TProgram extends string = typeof GHOST_WALLET_PROGRAM_ADDRESS,
  TAccountMetas extends readonly AccountMeta[] = readonly AccountMeta[],
> = {
  programAddress: Address<TProgram>;
  accounts: {
    funder: TAccountMetas[0];
    wallet: TAccountMetas[1];
    action: TAccountMetas[2];
    systemProgram: TAccountMetas[3];
  };
  data: FundGhostWalletInstructionData;
};

export function parseFundGhostWalletInstruction<
  TProgram extends string,
  TAccountMetas extends readonly AccountMeta[],
>(
  instruction: Instruction<TProgram> &
    InstructionWithAccounts<TAccountMetas> &
    InstructionWithData<ReadonlyUint8Array>,
): ParsedFundGhostWalletInstruction<TProgram, TAccountMetas> {
  if (instruction.accounts.length < 4) {
    // TODO: Coded error.
    throw new Error("Not enough accounts");
  }
  let accountIndex = 0;
  const getNextAccount = () => {
    const accountMeta = (instruction.accounts as TAccountMetas)[accountIndex]!;
    accountIndex += 1;
    return accountMeta;
  };
  return {
    programAddress: instruction.programAddress,
    accounts: {
      funder: getNextAccount(),
      wallet: getNextAccount(),
      action: getNextAccount(),
      systemProgram: getNextAccount(),
    },
    data: getFundGhostWalletInstructionDataDecoder().decode(instruction.data),
  };
}
