/**
 * This code was AUTOGENERATED using the Codama library.
 * Please DO NOT EDIT THIS FILE, instead use visitors
 * to add features, then rerun Codama to update it.
 *
 * @see https://github.com/codama-idl/codama
 */

import {
  addDecoderSizePrefix,
  addEncoderSizePrefix,
  combineCodec,
  fixDecoderSize,
  fixEncoderSize,
  getBytesDecoder,
  getBytesEncoder,
  getI64Decoder,
  getI64Encoder,
  getStructDecoder,
  getStructEncoder,
  getU32Decoder,
  getU32Encoder,
  getU64Decoder,
  getU64Encoder,
  getUtf8Decoder,
  getUtf8Encoder,
  transformEncoder,
  type AccountMeta,
  type AccountSignerMeta,
  type Address,
  type Codec,
  type Decoder,
  type Encoder,
  type Instruction,
  type InstructionWithAccounts,
  type InstructionWithData,
  type ReadonlyAccount,
  type ReadonlyUint8Array,
  type TransactionSigner,
  type WritableAccount,
  type WritableSignerAccount,
} from "@solana/kit";
import { findWalletPda } from "../pdas";
import { GHOST_WALLET_PROGRAM_ADDRESS } from "../programs";
import {
  expectAddress,
  expectSome,
  getAccountMetaFactory,
  type ResolvedAccount,
} from "../shared";

export const CREATE_GHOST_WALLET_DISCRIMINATOR = new Uint8Array([
  127, 149, 247, 34, 99, 76, 23, 190,
]);

export function getCreateGhostWalletDiscriminatorBytes() {
  return fixEncoderSize(getBytesEncoder(), 8).encode(
    CREATE_GHOST_WALLET_DISCRIMINATOR,
  );
}

export type CreateGhostWalletInstruction<
  TProgram extends string = typeof GHOST_WALLET_PROGRAM_ADDRESS,
  TAccountCreator extends string | AccountMeta<string> = string,
  TAccountWallet extends string | AccountMeta<string> = string,
  TAccountSystemProgram extends string | AccountMeta<string> =
    "11111111111111111111111111111111",
  TRemainingAccounts extends readonly AccountMeta<string>[] = [],
> = Instruction<TProgram> &
  InstructionWithData<ReadonlyUint8Array> &
  InstructionWithAccounts<
    [
      TAccountCreator extends string
        ? WritableSignerAccount<TAccountCreator> &
            AccountSignerMeta<TAccountCreator>
        : TAccountCreator,
      TAccountWallet extends string
        ? WritableAccount<TAccountWallet>
        : TAccountWallet,
      TAccountSystemProgram extends string
        ? ReadonlyAccount<TAccountSystemProgram>
        : TAccountSystemProgram,
      ...TRemainingAccounts,
    ]
  >;

export type CreateGhostWalletInstructionData = {
  discriminator: ReadonlyUint8Array;
  taskId: bigint;
  purpose: string;
  durationSeconds: bigint;
};

export type CreateGhostWalletInstructionDataArgs = {
  taskId: number | bigint;
  purpose: string;
  durationSeconds: number | bigint;
};

export function getCreateGhostWalletInstructionDataEncoder(): Encoder<CreateGhostWalletInstructionDataArgs> {
  return transformEncoder(
    getStructEncoder([
      ["discriminator", fixEncoderSize(getBytesEncoder(), 8)],
      ["taskId", getU64Encoder()],
      ["purpose", addEncoderSizePrefix(getUtf8Encoder(), getU32Encoder())],
      ["durationSeconds", getI64Encoder()],
    ]),
    (value) => ({ ...value, discriminator: CREATE_GHOST_WALLET_DISCRIMINATOR }),
  );
}

export function getCreateGhostWalletInstructionDataDecoder(): Decoder<CreateGhostWalletInstructionData> {
  return getStructDecoder([
    ["discriminator", fixDecoderSize(getBytesDecoder(), 8)],
    ["taskId", getU64Decoder()],
    ["purpose", addDecoderSizePrefix(getUtf8Decoder(), getU32Decoder())],
    ["durationSeconds", getI64Decoder()],
  ]);
}

export function getCreateGhostWalletInstructionDataCodec(): Codec<
  CreateGhostWalletInstructionDataArgs,
  CreateGhostWalletInstructionData
> {
  return combineCodec(
    getCreateGhostWalletInstructionDataEncoder(),
    getCreateGhostWalletInstructionDataDecoder(),
  );
}

export type CreateGhostWalletAsyncInput<
  TAccountCreator extends string = string,
  TAccountWallet extends string = string,
  TAccountSystemProgram extends string = string,
> = {
  creator: TransactionSigner<TAccountCreator>;
  wallet?: Address<TAccountWallet>;
  systemProgram?: Address<TAccountSystemProgram>;
  taskId: CreateGhostWalletInstructionDataArgs["taskId"];
  purpose: CreateGhostWalletInstructionDataArgs["purpose"];
  durationSeconds: CreateGhostWalletInstructionDataArgs["durationSeconds"];
};

export async function getCreateGhostWalletInstructionAsync<
  TAccountCreator extends string,
  TAccountWallet extends string,
  TAccountSystemProgram extends string,
  TProgramAddress extends Address = typeof GHOST_WALLET_PROGRAM_ADDRESS,
>(
  input: CreateGhostWalletAsyncInput<
    TAccountCreator,
    TAccountWallet,
    TAccountSystemProgram
  >,
  config?: { programAddress?: TProgramAddress },
): Promise<
  CreateGhostWalletInstruction<
    TProgramAddress,
    TAccountCreator,
    TAccountWallet,
    TAccountSystemProgram
  >
> {
  // Program address.
  const programAddress = config?.programAddress ?? GHOST_WALLET_PROGRAM_ADDRESS;

  // Original accounts.
  const originalAccounts = {
    creator: { value: input.creator ?? null, isWritable: true },
    wallet: { value: input.wallet ?? null, isWritable: true },
    systemProgram: { value: input.systemProgram ?? null, isWritable: false },
  };
  const accounts = originalAccounts as Record<
    keyof typeof originalAccounts,
    ResolvedAccount
  >;

  // Original args.
  const args = { ...input };

  // Resolve default values.
  if (!accounts.wallet.value) {
    accounts.wallet.value = await findWalletPda({
      creator: expectAddress(accounts.creator.value),
      taskId: expectSome(args.taskId),
    });
  }
  if (!accounts.systemProgram.value) {
    accounts.systemProgram.value =
      "11111111111111111111111111111111" as Address<"11111111111111111111111111111111">;
  }

  const getAccountMeta = getAccountMetaFactory(programAddress, "programId");
  return Object.freeze({
    accounts: [
      getAccountMeta(accounts.creator),
      getAccountMeta(accounts.wallet),
      getAccountMeta(accounts.systemProgram),
    ],
    data: getCreateGhostWalletInstructionDataEncoder().encode(
      args as CreateGhostWalletInstructionDataArgs,
    ),
    programAddress,
  } as CreateGhostWalletInstruction<
    TProgramAddress,
    TAccountCreator,
    TAccountWallet,
    TAccountSystemProgram
  >);
}

export type CreateGhostWalletInput<
  TAccountCreator extends string = string,
  TAccountWallet extends string = string,
  TAccountSystemProgram extends string = string,
> = {
  creator: TransactionSigner<TAccountCreator>;
  wallet: Address<TAccountWallet>;
  systemProgram?: Address<TAccountSystemProgram>;
  taskId: CreateGhostWalletInstructionDataArgs["taskId"];
  purpose: CreateGhostWalletInstructionDataArgs["purpose"];
  durationSeconds: CreateGhostWalletInstructionDataArgs["durationSeconds"];
};

export function getCreateGhostWalletInstruction<
  TAccountCreator extends string,
  TAccountWallet extends string,
  TAccountSystemProgram extends string,
  TProgramAddress extends Address = typeof GHOST_WALLET_PROGRAM_ADDRESS,
>(
  input: CreateGhostWalletInput<
    TAccountCreator,
    TAccountWallet,
    TAccountSystemProgram
  >,
  config?: { programAddress?: TProgramAddress },
): CreateGhostWalletInstruction<
  TProgramAddress,
  TAccountCreator,
  TAccountWallet,
  TAccountSystemProgram
> {
  // Program address.
  const programAddress = config?.programAddress ?? GHOST_WALLET_PROGRAM_ADDRESS;

  // Original accounts.
  const originalAccounts = {
    creator: { value: input.creator ?? null, isWritable: true },
    wallet: { value: input.wallet ?? null, isWritable: true },
    systemProgram: { value: input.systemProgram ?? null, isWritable: false },
  };
  const accounts = originalAccounts as Record<
    keyof typeof originalAccounts,
    ResolvedAccount
  >;

  // Original args.
  const args = { ...input };

  // Resolve default values.
  if (!accounts.systemProgram.value) {
    accounts.systemProgram.value =
      "11111111111111111111111111111111" as Address<"11111111111111111111111111111111">;
  }

  const getAccountMeta = getAccountMetaFactory(programAddress, "programId");
  return Object.freeze({
    accounts: [
      getAccountMeta(accounts.creator),
      getAccountMeta(accounts.wallet),
      getAccountMeta(accounts.systemProgram),
    ],
    data: getCreateGhostWalletInstructionDataEncoder().encode(
      args as CreateGhostWalletInstructionDataArgs,
    ),
    programAddress,
  } as CreateGhostWalletInstruction<
    TProgramAddress,
    TAccountCreator,
    TAccountWallet,
    TAccountSystemProgram
  >);
}

export type ParsedCreateGhostWalletInstruction<
  TProgram extends string = typeof GHOST_WALLET_PROGRAM_ADDRESS,
  TAccountMetas extends readonly AccountMeta[] = readonly AccountMeta[],
> = {
  programAddress: Address<TProgram>;
  accounts: {
    creator: TAccountMetas[0];
    wallet: TAccountMetas[1];
    systemProgram: TAccountMetas[2];
  };
  data: CreateGhostWalletInstructionData;
};

export function parseCreateGhostWalletInstruction<
  TProgram extends string,
  TAccountMetas extends readonly AccountMeta[],
>(
  instruction: Instruction<TProgram> &
    InstructionWithAccounts<TAccountMetas> &
    InstructionWithData<ReadonlyUint8Array>,
): ParsedCreateGhostWalletInstruction<TProgram, TAccountMetas> {
  if (instruction.accounts.length < 3) {
    // TODO: Coded error.
    throw new Error("Not enough accounts");
  }
  let accountIndex = 0;
  const getNextAccount = () => {
    const accountMeta = (instruction.accounts as TAccountMetas)[accountIndex]!;
    accountIndex += 1;
    return accountMeta;
  };
  return {
    programAddress: instruction.programAddress,
    accounts: {
      creator: getNextAccount(),
      wallet: getNextAccount(),
      systemProgram: getNextAccount(),
    },
    data: getCreateGhostWalletInstructionDataDecoder().decode(instruction.data),
  };
}
