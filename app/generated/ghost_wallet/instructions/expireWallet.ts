/**
 * This code was AUTOGENERATED using the Codama library.
 * Please DO NOT EDIT THIS FILE, instead use visitors
 * to add features, then rerun Codama to update it.
 *
 * @see https://github.com/codama-idl/codama
 */

import {
  combineCodec,
  fixDecoderSize,
  fixEncoderSize,
  getBytesDecoder,
  getBytesEncoder,
  getStructDecoder,
  getStructEncoder,
  transformEncoder,
  type AccountMeta,
  type AccountSignerMeta,
  type Address,
  type FixedSizeCodec,
  type FixedSizeDecoder,
  type FixedSizeEncoder,
  type Instruction,
  type InstructionWithAccounts,
  type InstructionWithData,
  type ReadonlyAccount,
  type ReadonlyUint8Array,
  type TransactionSigner,
  type WritableAccount,
  type WritableSignerAccount,
} from "@solana/kit";
import { GHOST_WALLET_PROGRAM_ADDRESS } from "../programs";
import { getAccountMetaFactory, type ResolvedAccount } from "../shared";

export const EXPIRE_WALLET_DISCRIMINATOR = new Uint8Array([
  56, 231, 47, 145, 42, 58, 50, 137,
]);

export function getExpireWalletDiscriminatorBytes() {
  return fixEncoderSize(getBytesEncoder(), 8).encode(
    EXPIRE_WALLET_DISCRIMINATOR,
  );
}

export type ExpireWalletInstruction<
  TProgram extends string = typeof GHOST_WALLET_PROGRAM_ADDRESS,
  TAccountCaller extends string | AccountMeta<string> = string,
  TAccountWallet extends string | AccountMeta<string> = string,
  TAccountAction extends string | AccountMeta<string> = string,
  TAccountSystemProgram extends string | AccountMeta<string> =
    "11111111111111111111111111111111",
  TRemainingAccounts extends readonly AccountMeta<string>[] = [],
> = Instruction<TProgram> &
  InstructionWithData<ReadonlyUint8Array> &
  InstructionWithAccounts<
    [
      TAccountCaller extends string
        ? WritableSignerAccount<TAccountCaller> &
            AccountSignerMeta<TAccountCaller>
        : TAccountCaller,
      TAccountWallet extends string
        ? WritableAccount<TAccountWallet>
        : TAccountWallet,
      TAccountAction extends string
        ? WritableAccount<TAccountAction>
        : TAccountAction,
      TAccountSystemProgram extends string
        ? ReadonlyAccount<TAccountSystemProgram>
        : TAccountSystemProgram,
      ...TRemainingAccounts,
    ]
  >;

export type ExpireWalletInstructionData = { discriminator: ReadonlyUint8Array };

export type ExpireWalletInstructionDataArgs = {};

export function getExpireWalletInstructionDataEncoder(): FixedSizeEncoder<ExpireWalletInstructionDataArgs> {
  return transformEncoder(
    getStructEncoder([["discriminator", fixEncoderSize(getBytesEncoder(), 8)]]),
    (value) => ({ ...value, discriminator: EXPIRE_WALLET_DISCRIMINATOR }),
  );
}

export function getExpireWalletInstructionDataDecoder(): FixedSizeDecoder<ExpireWalletInstructionData> {
  return getStructDecoder([
    ["discriminator", fixDecoderSize(getBytesDecoder(), 8)],
  ]);
}

export function getExpireWalletInstructionDataCodec(): FixedSizeCodec<
  ExpireWalletInstructionDataArgs,
  ExpireWalletInstructionData
> {
  return combineCodec(
    getExpireWalletInstructionDataEncoder(),
    getExpireWalletInstructionDataDecoder(),
  );
}

export type ExpireWalletInput<
  TAccountCaller extends string = string,
  TAccountWallet extends string = string,
  TAccountAction extends string = string,
  TAccountSystemProgram extends string = string,
> = {
  caller: TransactionSigner<TAccountCaller>;
  wallet: Address<TAccountWallet>;
  action: Address<TAccountAction>;
  systemProgram?: Address<TAccountSystemProgram>;
};

export function getExpireWalletInstruction<
  TAccountCaller extends string,
  TAccountWallet extends string,
  TAccountAction extends string,
  TAccountSystemProgram extends string,
  TProgramAddress extends Address = typeof GHOST_WALLET_PROGRAM_ADDRESS,
>(
  input: ExpireWalletInput<
    TAccountCaller,
    TAccountWallet,
    TAccountAction,
    TAccountSystemProgram
  >,
  config?: { programAddress?: TProgramAddress },
): ExpireWalletInstruction<
  TProgramAddress,
  TAccountCaller,
  TAccountWallet,
  TAccountAction,
  TAccountSystemProgram
> {
  // Program address.
  const programAddress = config?.programAddress ?? GHOST_WALLET_PROGRAM_ADDRESS;

  // Original accounts.
  const originalAccounts = {
    caller: { value: input.caller ?? null, isWritable: true },
    wallet: { value: input.wallet ?? null, isWritable: true },
    action: { value: input.action ?? null, isWritable: true },
    systemProgram: { value: input.systemProgram ?? null, isWritable: false },
  };
  const accounts = originalAccounts as Record<
    keyof typeof originalAccounts,
    ResolvedAccount
  >;

  // Resolve default values.
  if (!accounts.systemProgram.value) {
    accounts.systemProgram.value =
      "11111111111111111111111111111111" as Address<"11111111111111111111111111111111">;
  }

  const getAccountMeta = getAccountMetaFactory(programAddress, "programId");
  return Object.freeze({
    accounts: [
      getAccountMeta(accounts.caller),
      getAccountMeta(accounts.wallet),
      getAccountMeta(accounts.action),
      getAccountMeta(accounts.systemProgram),
    ],
    data: getExpireWalletInstructionDataEncoder().encode({}),
    programAddress,
  } as ExpireWalletInstruction<
    TProgramAddress,
    TAccountCaller,
    TAccountWallet,
    TAccountAction,
    TAccountSystemProgram
  >);
}

export type ParsedExpireWalletInstruction<
  TProgram extends string = typeof GHOST_WALLET_PROGRAM_ADDRESS,
  TAccountMetas extends readonly AccountMeta[] = readonly AccountMeta[],
> = {
  programAddress: Address<TProgram>;
  accounts: {
    caller: TAccountMetas[0];
    wallet: TAccountMetas[1];
    action: TAccountMetas[2];
    systemProgram: TAccountMetas[3];
  };
  data: ExpireWalletInstructionData;
};

export function parseExpireWalletInstruction<
  TProgram extends string,
  TAccountMetas extends readonly AccountMeta[],
>(
  instruction: Instruction<TProgram> &
    InstructionWithAccounts<TAccountMetas> &
    InstructionWithData<ReadonlyUint8Array>,
): ParsedExpireWalletInstruction<TProgram, TAccountMetas> {
  if (instruction.accounts.length < 4) {
    // TODO: Coded error.
    throw new Error("Not enough accounts");
  }
  let accountIndex = 0;
  const getNextAccount = () => {
    const accountMeta = (instruction.accounts as TAccountMetas)[accountIndex]!;
    accountIndex += 1;
    return accountMeta;
  };
  return {
    programAddress: instruction.programAddress,
    accounts: {
      caller: getNextAccount(),
      wallet: getNextAccount(),
      action: getNextAccount(),
      systemProgram: getNextAccount(),
    },
    data: getExpireWalletInstructionDataDecoder().decode(instruction.data),
  };
}
