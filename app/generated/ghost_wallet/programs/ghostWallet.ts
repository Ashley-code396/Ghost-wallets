/**
 * This code was AUTOGENERATED using the Codama library.
 * Please DO NOT EDIT THIS FILE, instead use visitors
 * to add features, then rerun Codama to update it.
 *
 * @see https://github.com/codama-idl/codama
 */

import {
  assertIsInstructionWithAccounts,
  containsBytes,
  fixEncoderSize,
  getBytesEncoder,
  type Address,
  type Instruction,
  type InstructionWithData,
  type ReadonlyUint8Array,
} from "@solana/kit";
import {
  parseCreateGhostWalletInstruction,
  parseExecuteActionInstruction,
  parseExpireWalletInstruction,
  parseFundGhostWalletInstruction,
  type ParsedCreateGhostWalletInstruction,
  type ParsedExecuteActionInstruction,
  type ParsedExpireWalletInstruction,
  type ParsedFundGhostWalletInstruction,
} from "../instructions";

export const GHOST_WALLET_PROGRAM_ADDRESS =
  "wsMSPLq1C2eB9BcjVxuzqfggWCvPVkHu1z81tFma7F3" as Address<"wsMSPLq1C2eB9BcjVxuzqfggWCvPVkHu1z81tFma7F3">;

export enum GhostWalletAccount {
  GhostAction,
  GhostWallet,
}

export function identifyGhostWalletAccount(
  account: { data: ReadonlyUint8Array } | ReadonlyUint8Array,
): GhostWalletAccount {
  const data = "data" in account ? account.data : account;
  if (
    containsBytes(
      data,
      fixEncoderSize(getBytesEncoder(), 8).encode(
        new Uint8Array([74, 16, 58, 47, 154, 151, 83, 141]),
      ),
      0,
    )
  ) {
    return GhostWalletAccount.GhostAction;
  }
  if (
    containsBytes(
      data,
      fixEncoderSize(getBytesEncoder(), 8).encode(
        new Uint8Array([123, 252, 153, 129, 219, 21, 219, 141]),
      ),
      0,
    )
  ) {
    return GhostWalletAccount.GhostWallet;
  }
  throw new Error(
    "The provided account could not be identified as a ghostWallet account.",
  );
}

export enum GhostWalletInstruction {
  CreateGhostWallet,
  ExecuteAction,
  ExpireWallet,
  FundGhostWallet,
}

export function identifyGhostWalletInstruction(
  instruction: { data: ReadonlyUint8Array } | ReadonlyUint8Array,
): GhostWalletInstruction {
  const data = "data" in instruction ? instruction.data : instruction;
  if (
    containsBytes(
      data,
      fixEncoderSize(getBytesEncoder(), 8).encode(
        new Uint8Array([127, 149, 247, 34, 99, 76, 23, 190]),
      ),
      0,
    )
  ) {
    return GhostWalletInstruction.CreateGhostWallet;
  }
  if (
    containsBytes(
      data,
      fixEncoderSize(getBytesEncoder(), 8).encode(
        new Uint8Array([246, 137, 105, 113, 247, 6, 223, 174]),
      ),
      0,
    )
  ) {
    return GhostWalletInstruction.ExecuteAction;
  }
  if (
    containsBytes(
      data,
      fixEncoderSize(getBytesEncoder(), 8).encode(
        new Uint8Array([56, 231, 47, 145, 42, 58, 50, 137]),
      ),
      0,
    )
  ) {
    return GhostWalletInstruction.ExpireWallet;
  }
  if (
    containsBytes(
      data,
      fixEncoderSize(getBytesEncoder(), 8).encode(
        new Uint8Array([119, 204, 155, 170, 80, 123, 87, 118]),
      ),
      0,
    )
  ) {
    return GhostWalletInstruction.FundGhostWallet;
  }
  throw new Error(
    "The provided instruction could not be identified as a ghostWallet instruction.",
  );
}

export type ParsedGhostWalletInstruction<
  TProgram extends string = "wsMSPLq1C2eB9BcjVxuzqfggWCvPVkHu1z81tFma7F3",
> =
  | ({
      instructionType: GhostWalletInstruction.CreateGhostWallet;
    } & ParsedCreateGhostWalletInstruction<TProgram>)
  | ({
      instructionType: GhostWalletInstruction.ExecuteAction;
    } & ParsedExecuteActionInstruction<TProgram>)
  | ({
      instructionType: GhostWalletInstruction.ExpireWallet;
    } & ParsedExpireWalletInstruction<TProgram>)
  | ({
      instructionType: GhostWalletInstruction.FundGhostWallet;
    } & ParsedFundGhostWalletInstruction<TProgram>);

export function parseGhostWalletInstruction<TProgram extends string>(
  instruction: Instruction<TProgram> & InstructionWithData<ReadonlyUint8Array>,
): ParsedGhostWalletInstruction<TProgram> {
  const instructionType = identifyGhostWalletInstruction(instruction);
  switch (instructionType) {
    case GhostWalletInstruction.CreateGhostWallet: {
      assertIsInstructionWithAccounts(instruction);
      return {
        instructionType: GhostWalletInstruction.CreateGhostWallet,
        ...parseCreateGhostWalletInstruction(instruction),
      };
    }
    case GhostWalletInstruction.ExecuteAction: {
      assertIsInstructionWithAccounts(instruction);
      return {
        instructionType: GhostWalletInstruction.ExecuteAction,
        ...parseExecuteActionInstruction(instruction),
      };
    }
    case GhostWalletInstruction.ExpireWallet: {
      assertIsInstructionWithAccounts(instruction);
      return {
        instructionType: GhostWalletInstruction.ExpireWallet,
        ...parseExpireWalletInstruction(instruction),
      };
    }
    case GhostWalletInstruction.FundGhostWallet: {
      assertIsInstructionWithAccounts(instruction);
      return {
        instructionType: GhostWalletInstruction.FundGhostWallet,
        ...parseFundGhostWalletInstruction(instruction),
      };
    }
    default:
      throw new Error(
        `Unrecognized instruction type: ${instructionType as string}`,
      );
  }
}
