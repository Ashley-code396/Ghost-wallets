/**
 * This code was AUTOGENERATED using the Codama library.
 * Please DO NOT EDIT THIS FILE, instead use visitors
 * to add features, then rerun Codama to update it.
 *
 * @see https://github.com/codama-idl/codama
 */

import {
  addDecoderSizePrefix,
  addEncoderSizePrefix,
  assertAccountExists,
  assertAccountsExist,
  combineCodec,
  decodeAccount,
  fetchEncodedAccount,
  fetchEncodedAccounts,
  fixDecoderSize,
  fixEncoderSize,
  getAddressDecoder,
  getAddressEncoder,
  getBytesDecoder,
  getBytesEncoder,
  getI64Decoder,
  getI64Encoder,
  getStructDecoder,
  getStructEncoder,
  getU32Decoder,
  getU32Encoder,
  getU64Decoder,
  getU64Encoder,
  getU8Decoder,
  getU8Encoder,
  getUtf8Decoder,
  getUtf8Encoder,
  transformEncoder,
  type Account,
  type Address,
  type Codec,
  type Decoder,
  type EncodedAccount,
  type Encoder,
  type FetchAccountConfig,
  type FetchAccountsConfig,
  type MaybeAccount,
  type MaybeEncodedAccount,
  type ReadonlyUint8Array,
} from "@solana/kit";

export const GHOST_WALLET_DISCRIMINATOR = new Uint8Array([
  123, 252, 153, 129, 219, 21, 219, 141,
]);

export function getGhostWalletDiscriminatorBytes() {
  return fixEncoderSize(getBytesEncoder(), 8).encode(
    GHOST_WALLET_DISCRIMINATOR,
  );
}

export type GhostWallet = {
  discriminator: ReadonlyUint8Array;
  creator: Address;
  taskId: bigint;
  purpose: string;
  createdAt: bigint;
  expiresAt: bigint;
  balance: bigint;
  status: number;
  bump: number;
  actionCount: bigint;
};

export type GhostWalletArgs = {
  creator: Address;
  taskId: number | bigint;
  purpose: string;
  createdAt: number | bigint;
  expiresAt: number | bigint;
  balance: number | bigint;
  status: number;
  bump: number;
  actionCount: number | bigint;
};

/** Gets the encoder for {@link GhostWalletArgs} account data. */
export function getGhostWalletEncoder(): Encoder<GhostWalletArgs> {
  return transformEncoder(
    getStructEncoder([
      ["discriminator", fixEncoderSize(getBytesEncoder(), 8)],
      ["creator", getAddressEncoder()],
      ["taskId", getU64Encoder()],
      ["purpose", addEncoderSizePrefix(getUtf8Encoder(), getU32Encoder())],
      ["createdAt", getI64Encoder()],
      ["expiresAt", getI64Encoder()],
      ["balance", getU64Encoder()],
      ["status", getU8Encoder()],
      ["bump", getU8Encoder()],
      ["actionCount", getU64Encoder()],
    ]),
    (value) => ({ ...value, discriminator: GHOST_WALLET_DISCRIMINATOR }),
  );
}

/** Gets the decoder for {@link GhostWallet} account data. */
export function getGhostWalletDecoder(): Decoder<GhostWallet> {
  return getStructDecoder([
    ["discriminator", fixDecoderSize(getBytesDecoder(), 8)],
    ["creator", getAddressDecoder()],
    ["taskId", getU64Decoder()],
    ["purpose", addDecoderSizePrefix(getUtf8Decoder(), getU32Decoder())],
    ["createdAt", getI64Decoder()],
    ["expiresAt", getI64Decoder()],
    ["balance", getU64Decoder()],
    ["status", getU8Decoder()],
    ["bump", getU8Decoder()],
    ["actionCount", getU64Decoder()],
  ]);
}

/** Gets the codec for {@link GhostWallet} account data. */
export function getGhostWalletCodec(): Codec<GhostWalletArgs, GhostWallet> {
  return combineCodec(getGhostWalletEncoder(), getGhostWalletDecoder());
}

export function decodeGhostWallet<TAddress extends string = string>(
  encodedAccount: EncodedAccount<TAddress>,
): Account<GhostWallet, TAddress>;
export function decodeGhostWallet<TAddress extends string = string>(
  encodedAccount: MaybeEncodedAccount<TAddress>,
): MaybeAccount<GhostWallet, TAddress>;
export function decodeGhostWallet<TAddress extends string = string>(
  encodedAccount: EncodedAccount<TAddress> | MaybeEncodedAccount<TAddress>,
): Account<GhostWallet, TAddress> | MaybeAccount<GhostWallet, TAddress> {
  return decodeAccount(
    encodedAccount as MaybeEncodedAccount<TAddress>,
    getGhostWalletDecoder(),
  );
}

export async function fetchGhostWallet<TAddress extends string = string>(
  rpc: Parameters<typeof fetchEncodedAccount>[0],
  address: Address<TAddress>,
  config?: FetchAccountConfig,
): Promise<Account<GhostWallet, TAddress>> {
  const maybeAccount = await fetchMaybeGhostWallet(rpc, address, config);
  assertAccountExists(maybeAccount);
  return maybeAccount;
}

export async function fetchMaybeGhostWallet<TAddress extends string = string>(
  rpc: Parameters<typeof fetchEncodedAccount>[0],
  address: Address<TAddress>,
  config?: FetchAccountConfig,
): Promise<MaybeAccount<GhostWallet, TAddress>> {
  const maybeAccount = await fetchEncodedAccount(rpc, address, config);
  return decodeGhostWallet(maybeAccount);
}

export async function fetchAllGhostWallet(
  rpc: Parameters<typeof fetchEncodedAccounts>[0],
  addresses: Array<Address>,
  config?: FetchAccountsConfig,
): Promise<Account<GhostWallet>[]> {
  const maybeAccounts = await fetchAllMaybeGhostWallet(rpc, addresses, config);
  assertAccountsExist(maybeAccounts);
  return maybeAccounts;
}

export async function fetchAllMaybeGhostWallet(
  rpc: Parameters<typeof fetchEncodedAccounts>[0],
  addresses: Array<Address>,
  config?: FetchAccountsConfig,
): Promise<MaybeAccount<GhostWallet>[]> {
  const maybeAccounts = await fetchEncodedAccounts(rpc, addresses, config);
  return maybeAccounts.map((maybeAccount) => decodeGhostWallet(maybeAccount));
}
